
language: ruby
rvm:
- 2.2.0
# branch whitelist
branches:
only:
- master
install:
- bundle install
script:
- rake build:drafts
- rake build
after_success:
- rake deploy:travisStaging
- rake deploy:travisProduction